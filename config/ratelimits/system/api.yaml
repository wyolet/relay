apiVersion: relay.wyolet.dev/v1
kind: RateLimit
metadata:
  name: api
  displayName: API rate limits
spec:
  source: system_mirrored
  description: "DoS protection for control and non-inference public API endpoints. Per-IP runtime cap."
  window: 1m
  strategy: sliding-window
  enabled: false
  rules:
    - meter: requests
      amount: 10
      window: 1s
      strategy: sliding-window
    - meter: requests
      amount: 300
      window: 1m
      strategy: sliding-window
