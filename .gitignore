.idea/
.vscode/
.DS_Store
*.swp
*.swo

# py-relay
py-relay/.venv/
py-relay/__pycache__/
py-relay/.python-version

# Build artifacts
/relay
/dist/
*.test
*.out

# UI dist — fetched at build time via make ui-fetch, not source-tracked
cmd/relay/web/dist/

# Local event logs
events/
*.jsonl

# Bench output — written at runtime, not source-tracked
bench/results*.json

# Secrets — never commit
.env
.env.*
!.env.example
