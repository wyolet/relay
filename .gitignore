.idea/
.vscode/
.DS_Store
*.swp
*.swo

# Local scratch dir (holds secrets like build tokens) — never commit
.tmp/

# Build artifacts
/relay
/dist/
*.test
*.out

# UI dist — fetched at build time via make ui-fetch, not source-tracked
cmd/relay/web/dist/

# Local event logs
events/
*.jsonl

# Kept out of the public repo (live copies in .tmp/) — moving to separate repos
/bench/
/infra/
/py-relay/

# Secrets — never commit
.env
.env.*
!.env.example

/replay
error.log
/ollama-models-dump.json

# symbol MCP plugin local index cache
.symbol/

# Local batch payload store (jobq FileStore default dir)
relay-batch-payloads/

# stray go-build artifacts of cmd/*
/modelsdev-import
/catalog-embed
