package main

// UIVersion is the pinned relay-ui release to embed.
// PER-272 will publish the v0.0.1 tag in the wyolet/relay-ui repo;
// bump this constant when a new UI release is ready.
const UIVersion = "v0.0.1"
